# Keep the build context minimal: only server.js, game/, public/ and the
# package manifests are needed (see COPY statements in the Dockerfile).
.git
.github
node_modules
data
test
k8s
docker
scripts
landing
docs
*.md
.dockerignore
docker-compose*.yml
Dockerfile
