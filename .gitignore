node_modules/
*.log
.DS_Store
data/*.json
# SQLite database + WAL/SHM sidecars (local account store, node:sqlite)
data/*.db
data/*.db-shm
data/*.db-wal
.env
docker/secrets/*.txt
docker/.env
# Secrets safety net (see scripts/check-secret-files.js, CI job secret-scan):
# any .env variant anywhere, keys and Terraform variables. Templates stay in.
**/.env
**/.env.*
!**/*.example
*.pem
*.key
*.p12
*.pfx
id_rsa
id_ed25519
id_ecdsa
terraform/*.tfvars
terraform/*.tfvars.json

# RL training artifacts (the ONNX models ARE committed - public repo, run-anywhere)
.venv/
python/__pycache__/
models/*.zip

# Human move logs for imitation learning (data collection)
data/human-moves.jsonl
docs/_build/
terraform/*.tfstate*
terraform/.terraform/
